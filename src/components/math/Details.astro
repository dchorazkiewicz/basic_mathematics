---
interface Props {
  summary?: string;
  open?: boolean;
}

const { summary = 'Pokaż szczegóły', open = false } = Astro.props;
---

<details class="math-details" open={open}>
  <summary>{summary}</summary>
  <div class="math-details__content">
    <slot />
  </div>
</details>
